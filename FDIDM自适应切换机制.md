# FDIDM 自适应参数切换机制说明

> 本文档描述软件仿真页（FDIDM Tab）中 α/β 自适应搜索与切换的完整机制，包括线程架构、逐帧流程、参数含义、判决逻辑、已知问题与优化方向。

## 1. 概述

FDIDM 有两个可调参数 **α** 和 **β**（取值范围 0~2）。不同取值对应不同的分数傅里叶基：

- α=β=0 时近似 OFDM；
- α=β=1 时近似 OTFS；
- 其他取值是介于两者之间的通用 chirp 波形。

LEO 卫星信道的时延、多普勒会随时间变化，不同信道下最优的 (α, β) 不同。自适应系统的目标就是：**持续观察信道，自动搜索并切换到性能更好的 (α, β)**，从而降低误码率（SER）。

整体采用「双时间尺度」设计：

| 时间尺度 | 做什么 |
| --- | --- |
| 快（每帧） | 仿真链路、更新信道/接收机、采集 CSI 快照、实时显示 |
| 慢（每若干帧） | 基于一段 CSI 窗口搜索最优 α/β，满足滞回条件后切换 |

## 2. 线程架构

当前实现共三条线程协同工作：

| 线程 | 职责 |
| --- | --- |
| 仿真线程 | 逐帧运行真实链路仿真（生成比特 → 映射 → 过信道 → 解码 → 统计），每帧提交一份 CSI 快照 |
| `fdidm-display-metric` | 消费度量快照，计算 OFDM/OTFS/AFDM/FDIDM 四波形理论 SER，限速+平滑后输出给右下角时间图 |
| `fdidm-slow-search` | 消费代表快照集，执行 α/β 粗/细网格搜索，做稳定性/增益/冷却判决，必要时切换参数 |

> 早期版本是「单 worker 线程」：同一个线程既出显示点又跑搜索，搜索期间右下角曲线会长时间不更新（表现为两个点间隔很大）。现已拆成两条独立线程 + 两个独立唤醒事件，搜索再慢也不会阻塞出点。

## 3. 完整流程

```text
仿真帧 (每帧)
   │
   ▼
_simulate_one_frame：锁内完成一帧链路仿真
   │
   ▼
_adaptive_note_frame_processed：提交 CSI 快照
   │
   ├──► 快照入窗（最近 window_frames 帧）
   │
   ├──► 每 benchmark_interval_frames 帧排队一个度量快照
   │        └──► 显示度量线程：evaluate_all 四波形 SER
   │                 └──► 0.5s 限速 + log-EMA 平滑 → 右下角时间图
   │
   └──► 窗口攒满且距上次排队 ≥ interval_frames 帧
            └──► 抽取 ensemble_snapshots 个代表快照
                  └──► 搜索线程：粗搜(0.5) → 细搜(0.1) → 推荐 (α*, β*) + 预测增益
                        └──► 判决：稳定次数 / 增益 / 参数差 / 冷却
                              └──► 满足 → set_indices 原子切换 → 记 switch 事件
                                    └──► UI 每 250ms 轮询：图上标注切换点
```

### 3.1 逐帧步骤

1. **仿真一帧**：`_simulate_one_frame` 在 `self._lock` 内完成比特生成、调制、过信道、解调、硬判、统计，帧末调用 `_adaptive_note_frame_processed`。
2. **提交快照**：快照内容包含 H_TF 矩阵、M/N、SNR、当前 α/β、帧号、物理时间等。
3. **入窗**：快照按每帧 1 个的节奏进入有界窗口（默认保留最近 32 帧）。
4. **显示度量**：每 4 帧排队一个度量快照并唤醒显示线程；显示线程用 `SharedWaveformBenchmark.evaluate_all` 在**同一个 H** 上算四种波形的理论 SER，经 0.5 秒限速 + log 域 EMA 平滑后写入历史，供右下角时间图绘制。
5. **触发搜索**：窗口攒满（默认 32 帧）且距上次排队 ≥ 16 帧时，从窗口均匀抽取 3 个代表快照，排队并唤醒搜索线程。
6. **搜索**：对代表快照集做联合优化——粗搜（步长 0.5）遍历 α/β 网格，再在最优附近细搜（步长 0.1），返回推荐参数与相对当前参数的预测增益。
7. **判决**：见第 5 节。全部条件满足且开启自动应用时，在后台锁内复查参数未被改动，然后调用 `set_indices` 原子写入新 α/β，并追加一条 `switch` 历史事件；否则追加 `eval` 事件及未切换原因。
8. **展示**：UI 定时器每 250ms 读取历史，右下角时间图绘制 SER 曲线，并在真实切换时刻标注新的 `(α, β)`；「自适应过程」页展示 SER-SNR 曲线、推荐状态、搜索耗时等。

## 4. 关键参数及默认值（敏捷档）

### 4.1 慢时间尺度自适应参数

| 参数 | 默认值 | 含义 |
| --- | --- | --- |
| 启用慢时标自适应 | 开 | 是否运行 α/β 搜索闭环 |
| 稳定后自动应用 | 开 | 推荐稳定后是否自动切换参数 |
| CSI窗口（帧） | 32 | 构造慢时标信道统计所用的最近帧数 |
| 代表快照数 | 3 | 从窗口均匀抽取多少个代表快照做联合优化 |
| 优化间隔（帧） | 16 | 每隔多少帧允许排队一次搜索 |
| 采样间隔（帧） | 4 | 每隔多少帧算一次四波形共享信道 SER 样本（显示数据源） |
| 图形显示周期（秒） | 0.5 | 时间图最多按此周期加一个点，不影响后台仿真速度 |
| 粗搜索步长 | 0.5 | 第一轮网格搜索步长 |
| 细搜索步长 | 0.1 | 最优邻域精化步长 |
| 稳定次数 | 2 | 连续多少次推荐同一参数区域后才允许切换 |
| 最小增益（dB） | 0.2 | 推荐参数相对当前参数的预测增益下限 |
| 切换冷却（帧） | 32 | 一次真实切换后至少保持多少帧 |
| 判决 SER 下限 | 1e-8 | 低于此 SER 视为候选几乎等价，防止高 SNR 下假切换 |

### 4.2 按 NTN 模型自动推导的窗口

启动或切换 NTN 模型时，代码会按相干时间自动推导窗口/间隔/冷却（`_sync_profile_defaults`）：

| NTN 模型 | 相干帧数 | CSI窗口 | 优化间隔 | 冷却 |
| --- | --- | --- | --- | --- |
| NTN-TDLA100-200（200 Hz） | ≈79 | 40 | 20 | 40 |
| NTN-TDLC5-1200（1200 Hz） | ≈13 | 16 | 8 | 16 |

推导公式：`Tc≈0.423/fD`，`窗口≈0.5×相干帧数`，`间隔≈0.5×窗口`，`冷却=窗口`。

### 4.3 旧默认（保守）与现默认（敏捷）对比

| 参数 | 保守 | 敏捷（现默认） |
| --- | --- | --- |
| CSI窗口 | 128 | 32 |
| 优化间隔 | 64 | 16 |
| 代表快照数 | 6 | 3 |
| 稳定次数 | 3 | 2 |
| 最小增益 | 0.5 dB | 0.2 dB |
| 切换冷却 | 128 | 32 |

## 5. 判决与滞回逻辑

一次搜索完成后，是否切换由以下条件共同决定：

```text
ready = 稳定次数 ≥ 稳定要求（默认 2）
        且 预测增益 ≥ 最小增益（默认 0.2 dB）
        且 参数差 ≥ 0.5 × 细步长（默认 0.05）
        且 距上次真实切换 ≥ 冷却帧数（默认 32）
```

未满足时按原因记录动作：

| 动作 | 含义 |
| --- | --- |
| `apply` | 条件全满足，执行切换 |
| `stable_pending` | 推荐还不稳定，等待更多次一致推荐 |
| `gain_below` | 预测增益不足 |
| `cooldown` | 距上次切换太近 |
| `keep` | 推荐与当前参数几乎相同 |
| `ready_manual` | 条件满足但未开启自动应用，等待手动操作 |

这些滞回门限是为了防止参数在相邻候选间来回振荡；代价是对快时变信道的反应偏慢（已通过敏捷默认参数缓解）。

## 6. 切换如何生效与显示

1. **原子生效**：搜索线程在 `self._lock` 内复查参数未变化后调用 `set_indices`，新 α/β 在下一帧仿真开始时生效（切换落在两帧之间）。
2. **曲线自动跳变**：右下角时间图上的 FDIDM 线是「按当前已应用 (α, β) 计算的理论 SER」，所以切换后下一个点自动落到新参数对应的 SER。
3. **图上标注**：真实的 `switch` 事件会在曲线上画紫色文字标注，显示切换时刻和新的 `(α, β)`。
4. **只标真实切换**：尚未通过稳定/增益/冷却判决的推荐只记 `eval` 事件，不标注时间图。

## 7. 四波形基准模型（为什么 OTFS 一直领先）

右下角四条线由 `SharedWaveformBenchmark` 在**同一个 H、同一 SNR、同一调制**下计算：

- OFDM：单抽头均衡（保留系数=1），非对角时延-多普勒耦合视为干扰；每符号付一次 CP（效率 ≈8/9）。
- OTFS / AFDM / FDIDM：保留每行最强 `2×径数+1` 个系数的稀疏 MMSE（3 径时=7）；每帧只付一次保护间隔（效率 ≈64/65）。
- 被省略的信道系数不会被无视，而是作为有色干扰加入 LMMSE 正则项。

实测（8×8、16QAM、10 dB，TDLA100-200）：

| 波形 | SER | 保留能量占比 | CP 效率 |
| --- | --- | --- | --- |
| OFDM | 3.58e-2 | 0.992 | 0.889 |
| AFDM | 5.43e-2 | 0.984 | 0.985 |
| OTFS | 1.41e-2 | 0.998 | 0.985 |
| FDIDM(0.8,1) | 1.34e-2 | 0.999 | 0.985 |

结论：OTFS 领先主要是因为「接收机预算 + CP 开销 + 延迟-多普勒域与信道结构天然匹配」这三者的组合，属于模型设定下的结果；优化后的 FDIDM 可以追平甚至略超 OTFS。

## 8. 实测效果

| 配置 | 评估次数 | 切换次数 | 首次切换帧 |
| --- | --- | --- | --- |
| 旧保守默认（128 窗口，700 帧） | 3 | 1 | frame 593（约 30+ 秒） |
| 敏捷默认（32 窗口，450 帧） | 5 | 1 | frame 247（GUI 节奏约 5~10 秒） |

单次搜索耗时是当前主要瓶颈：8×8、3 快照时约 3~4 秒，6 快照时约 10 秒。

## 9. 已知问题

1. **搜索耗时成为瓶颈**：待处理搜索是单槽位，搜索期间新排队会被最新快照覆盖，有效评估频率≈搜索耗时，而非优化间隔。
2. **GIL 限制**：线程拆分解决了「搜索期间出点卡顿」，但 Python 线程不能真正并行计算，搜索与仿真仍在抢 CPU（大 M/N 时帧率下降）。
3. **显示的是理论 SER**：时间图是理论值，不是帧实测误码率；高 SNR 下两者可能明显偏离。
4. **波形排序是模型产物**：OFDM 单抽头+CP、AFDM 参数固定，导致 OTFS 一直领先，不代表绝对物理结论。
5. **显示依赖自适应开关**：关闭「启用慢时标自适应」后右下角时间图会停止加新点。
6. **仓库遗留测试失败**：部分测试只跑 8~25 帧、连窗口都攒不满；另有 UI 回归用例引用不存在的接口。
7. **硬件页未同步**：USRP 硬件自适应是另一套实现和参数，未同步敏捷默认值。

## 10. 优化方向

1. **搜索进进程池**：把搜索放进 `ProcessPoolExecutor`，numpy 矩阵运算真正多核并行，单次搜索从秒级降到亚秒级。
2. **搜索提速**：按上下文缓存 α/β 评估结果；用上次推荐做热启动只搜邻域；粗搜阶段按增益门限提前终止。
3. **改进队列语义**：搜索期间新排队先攒着而非覆盖，保证每轮评估都不白等。
4. **显示与开关解耦**：自适应关闭时曲线继续出点，或在界面上明确提示「曲线已暂停」。
5. **理论/实测双线显示**：时间图同时画理论 SER 与实测窗口 SER，便于验证切换增益。
6. **预设模式**：把「保守/敏捷」做成 UI 下拉预设。
7. **AFDM 参数自适应**：把 chirp 参数 c1/c2 纳入优化，消除「OTFS 永远赢」的模型偏差。
8. **收尾工程**：修复遗留测试、补齐线程并发回归测试、同步硬件页参数。

## 11. 相关代码位置

- 自适应闭环与线程：`waveform_sim/simulation/fdidm_adaptive.py`
- 仿真后端：`waveform_sim/simulation/simple_fdidm_rx.py`
- 四波形共享基准：`waveform_sim/simulation/shared_waveform_benchmark.py`
- 显示降采样：`waveform_sim/simulation/display_metrics.py`
- UI（时间图/信息面板/同步默认值）：`waveform_sim/ui/fdidm_tab.py`
- 自适应控件：`waveform_sim/ui/fdidm_adaptive_widgets.py`
- 统一配置：`waveform_sim/core/config.py`
